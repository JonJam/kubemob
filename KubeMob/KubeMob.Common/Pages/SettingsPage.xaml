<?xml version="1.0" encoding="utf-8" ?>
<base:ExtendedContentPage xmlns="http://xamarin.com/schemas/2014/forms"
                          xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                          xmlns:markupExtensions="clr-namespace:KubeMob.Common.MarkupExtensions;assembly=KubeMob.Common"
                          xmlns:base="clr-namespace:KubeMob.Common.Pages.Base;assembly=KubeMob.Common"
                          xmlns:common="clr-namespace:KubeMob.Common;assembly=KubeMob.Common"
                          x:Class="KubeMob.Common.Pages.SettingsPage"
             Title="{markupExtensions:Translate Settings}"
             common:ViewModelLocator.AutoWireViewModel="true">
<!--TODO Update bindings-->
    <TableView Intent="Settings">
        <TableRoot>
            <TableSection Title="{markupExtensions:Translate Workloads}">
                <SwitchCell Text="{markupExtensions:Translate CronJobs}" On="{Binding KubernetesService.ShowCronJobs, Mode=TwoWay}"/>
                <SwitchCell Text="{markupExtensions:Translate DaemonSets}" On="{Binding KubernetesService.ShowDaemonSets, Mode=TwoWay}" />
                <SwitchCell Text="{markupExtensions:Translate Deployments}" On="{Binding KubernetesService.ShowDeployments, Mode=TwoWay}" />
                <SwitchCell Text="{markupExtensions:Translate Jobs}" On="{Binding KubernetesService.ShowJobs, Mode=TwoWay}" />
                <SwitchCell Text="{markupExtensions:Translate Pods}" On="{Binding KubernetesService.ShowPods, Mode=TwoWay}" />
                <SwitchCell Text="{markupExtensions:Translate ReplicaSets}" On="{Binding KubernetesService.ShowReplicaSets, Mode=TwoWay}" />
                <SwitchCell Text="{markupExtensions:Translate ReplicationControllers}" On="{Binding KubernetesService.ShowReplicationControllers, Mode=TwoWay}" />
                <SwitchCell Text="{markupExtensions:Translate StatefulSets}" On="{Binding KubernetesService.ShowStatefulSets, Mode=TwoWay}" />
            </TableSection>
            <TableSection Title="{markupExtensions:Translate DiscoveryAndLoadBalancing}">
                <SwitchCell Text="{markupExtensions:Translate Ingresses}" On="{Binding KubernetesService.ShowIngresses, Mode=TwoWay}" />
                <SwitchCell Text="{markupExtensions:Translate Services}" On="{Binding KubernetesService.ShowServices, Mode=TwoWay}" />
            </TableSection>
            <TableSection Title="{markupExtensions:Translate ConfigAndStorage}">
                <SwitchCell Text="{markupExtensions:Translate ConfigMaps}" On="{Binding KubernetesService.ShowConfigMaps, Mode=TwoWay}" />
                <SwitchCell Text="{markupExtensions:Translate PersistentVolumeClaims}" On="{Binding KubernetesService.ShowPersistentVolumeClaims, Mode=TwoWay}" />
                <SwitchCell Text="{markupExtensions:Translate Secrets}" On="{Binding KubernetesService.ShowSecrets, Mode=TwoWay}" />
            </TableSection>
        </TableRoot>
    </TableView>
</base:ExtendedContentPage>